{
  "name": "project-skyring",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "lint": "eslint .",
    "lint:fix": "npm run lint -- --fix",
    "tap": "tap",
    "test": "npm run local tap -- --coverage-report=text --coverage-report=json",
    "lint:fix": "npm run lint -- --fix",
    "local": "env $(cat local.env)",
    "docs": "jsdoc -c jsdoc.json && apidoc -i packages/skyring/lib/server -o docs/api",
    "check": "check-pkg -d !docs -d !node_modules -d !examples -d !.git",
    "start": "npm run compose:up",
    "stop": "npm run compose:down"
  },
  "devDependencies": {
    "apidoc": "^0.17.6",
    "chalk": "^3.0.0",
    "ink-docstrap": "^1.3.2",
    "jsdoc": "^3.5.5",
    "lerna": "^3.20.2",
    "nats": "^1.4.8",
    "tap": "^14.10.7"
  },
  "dependencies": {
    "get-port": "^5.1.1"
  }
}
