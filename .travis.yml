sudo: required
services:
  - docker

install: npm install
script: npm run test:ci
language: node_js
after_install:
  - docker-compose -f compose/test.yml down
git:
  depth: 3
node_js:
  - "6"
  - "7"
